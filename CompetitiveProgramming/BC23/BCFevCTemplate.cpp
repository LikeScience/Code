#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0); cin.tie(0);
    fstream fin("fire.in", fstream::in);
    fstream fout("fire.out", fstream::out);
    //utiliza fin e fout exatamente como utilizarias cin e cout
    //exemplo: fin >> n; fout << n << '\n';

    fin.close();
    fout.close();
}