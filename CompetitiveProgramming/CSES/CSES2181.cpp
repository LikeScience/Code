#include <bits/stdc++.h>
#define F(i,s,e) for (int i = s; i<e; i++)
using namespace std;
#define ll long long
ll M = 1e9+7;
int n, m;
vector<vector<bool> > k, n(1024,vector<int>(1024,0));
int main() {
    ios::sync_with_stdio(0); cin.tie(0);
    int n, m; cin >> n >> m; 
    int i = 1;
    k[0][0]=1;
    while (i < m-1) {
        

        i++;
    }
}   