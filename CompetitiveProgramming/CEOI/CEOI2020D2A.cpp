#include <bits/stdc++.h>

#define F(i,s,e) for(int i = s; i < e; i++)
using namespace std;

int main() {
    int N, D, U, Q;
    vector<vector<pair
    cin >> N >> D >> U >> Q;
    F(i,0,N) {
        cin 
    }
}